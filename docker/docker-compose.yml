services:
  crawler:
    build:
      context: ../crawler
      dockerfile: Dockerfile
    hostname: pc-crawler
    container_name: pc-crawler
    expose:
      - 8080
    tty: true
    volumes:
      - ../crawler:/workspace
      - paper-crawler-volume:/volume

volumes:
  paper-crawler-volume:
    driver: local